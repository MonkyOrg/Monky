{
  "name": "@monky/bot-sdk",
  "version": "1.0.0",
  "description": "SDK for building Monky bots with slash commands",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "monky-bot-sdk": "./dist/tools.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "npm run build && npm run test:types && node --test",
    "test:types": "tsc -p tsconfig.type-tests.json"
  },
  "dependencies": {
    "@monky/shared": "*",
    "tar": "^7.5.22",
    "werift": "^0.24.4",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "@types/ws": "^8.5.0",
    "typescript": "^5.3.0"
  },
  "license": "GPL-3.0-or-later"
}
