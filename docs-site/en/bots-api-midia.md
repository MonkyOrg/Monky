---
outline: 2
---
<!-- Generated by docs-site/scripts/generate-bot-reference.mjs. Do not edit by hand. -->

# Media and local execution types

Complete public signatures of the Monky bot SDK, generated from the TypeScript entry point of `@monky/bot-sdk`. Optional fields use `?`; `readonly` fields must not be mutated. The source links include validation rules and implementation details.

[Bot SDK overview and events](/en/bots-api) · [Voice](/en/bots-voz) · [Miniapps](/en/bots-miniapps) · [Local execution](/en/bots-execucao-local)

## `BotScreen` {#botscreen}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/botScreens.ts#L102)

```ts
export type BotScreen = {
    revision: number;
    id: string;
    title: string;
    botId: string;
    state: BotScreenJson;
    channelId: string;
    createdAt: number;
    html: string;
    instanceId: string;
    creatorUserId?: string | undefined;
};
```

## `BotScreenActionEvent` {#botscreenactionevent}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/botScreens.ts#L107)

```ts
export type BotScreenActionEvent = {
    revision: number;
    channelId: string;
    userId: string;
    instanceId: string;
    action: string;
    payload: BotScreenJson;
    actionId: string;
    screenId: string;
    userNickname: string;
};
```

## `BotScreenClient` {#botscreenclient}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/bot-sdk/src/BotScreenClient.ts#L15)

```ts
export class BotScreenClient {
    constructor(adapter: BotScreenAdapter);
    createScreen(serverId: string, input: BotScreenCreate): Promise<BotScreen>;
    updateScreen(serverId: string, ref: BotScreenRef, patch: BotScreenPatch): Promise<BotScreen>;
    closeScreen(serverId: string, ref: BotScreenRef): Promise<void>;
    listScreens(serverId: string, channelId: string): Promise<BotScreen[]>;
    handle(serverId: string, type: string, payload: unknown): boolean;
}
```

## `BotScreenCreate` {#botscreencreate}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/botScreens.ts#L104)

```ts
export type BotScreenCreate = {
    title: string;
    state: BotScreenJson;
    channelId: string;
    html: string;
    invocationId?: string | undefined;
    id?: string | undefined;
};
```

## `BotScreenJson` {#botscreenjson}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/botScreens.ts#L16)

```ts
export type BotScreenJson = string | number | boolean | BotScreenJson[] | {
    [key: string]: BotScreenJson;
} | null;
```

## `BotScreenPatch` {#botscreenpatch}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/botScreens.ts#L105)

```ts
export type BotScreenPatch = {
    state: BotScreenJson;
    expectedRevision: number;
};
```

## `BotScreenRef` {#botscreenref}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/botScreens.ts#L103)

```ts
export type BotScreenRef = {
    id: string;
    instanceId: string;
};
```

## `BotScreenRemoved` {#botscreenremoved}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/botScreens.ts#L108)

```ts
export type BotScreenRemoved = {
    id: string;
    reason: "ended";
    channelId: string;
    instanceId: string;
    endedByUserId: string;
} | {
    id: string;
    reason: "closed";
    channelId: string;
    instanceId: string;
} | {
    id: string;
    reason: "access_revoked";
    channelId: string;
    instanceId: string;
} | {
    id: string;
    reason: "bot_disconnected";
    channelId: string;
    instanceId: string;
} | {
    id: string;
    reason: "view_revoked";
    channelId: string;
    instanceId: string;
};
```

## `BotVoiceAudioReceiver` {#botvoiceaudioreceiver}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/bot-sdk/src/voice/VoiceAudioReceiver.ts#L17)

```ts
export interface BotVoiceAudioReceiver {
    readonly droppedPackets: number;
    return: () => Promise<IteratorResult<BotVoicePacket, undefined>>;
    throw: (error: unknown) => Promise<IteratorResult<BotVoicePacket, undefined>>;
    [Symbol.asyncIterator]: () => AsyncIterableIterator<import("C:/Projetos/Monky-batch-670-682/packages/bot-sdk/src/index").BotVoicePacket, undefined, undefined>;
    next: (...[value]: [
    ] | [
        undefined
    ]) => Promise<IteratorResult<import("C:/Projetos/Monky-batch-670-682/packages/bot-sdk/src/index").BotVoicePacket, undefined>>;
}
```

## `BotVoiceConnection` {#botvoiceconnection}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/bot-sdk/src/voice/BotVoiceConnection.ts#L32)

```ts
export class BotVoiceConnection {
    constructor(channelId: string, auth: BotVoiceAuth, callbacks: VoiceCallbacks, publishAudio?: boolean);
    readonly channelId: string;
    get humanParticipantCount(): number;
    get isClosed(): boolean;
    get isReceivingAudio(): boolean;
    get receivesAudio(): boolean;
    join(options?: BotVoiceJoinOptions): Promise<void>;
    receiveAudio(argument0?: {
        signal?: AbortSignal;
    }): BotVoiceAudioReceiver;
    setMuted(muted: boolean): Promise<void>;
    setDeafened(deafened: boolean): Promise<void>;
    handle(message: VoiceMessage): boolean;
    writeOpus(frame: Uint8Array): Promise<void>;
    stopSpeaking(): void;
    close(): Promise<void>;
    disconnect(reason: string): Promise<void>;
}
```

## `BotVoiceJoinOptions` {#botvoicejoinoptions}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/botVoice.ts#L15)

```ts
export type BotVoiceJoinOptions = {
    invocationId?: string | undefined;
    receiveAudio?: boolean | undefined;
};
```

## `BotVoicePacket` {#botvoicepacket}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/bot-sdk/src/voice/VoiceAudioReceiver.ts#L1)

```ts
export interface BotVoicePacket {
    channelId: string;
    userId: string;
    sessionId: string;
    codec: 'opus';
    clockRate: 48000;
    channels: 2;
    /** Raw Opus, not PCM or an Ogg container. Owned by this packet's caller. */
    opus: Uint8Array;
    sequenceNumber: number;
    timestamp: number;
    ssrc: number;
    /** Monotonic milliseconds in this process, not wall time or RTP time. */
    receivedAt: number;
}
```

## `LocalCapabilityId` {#localcapabilityid}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/localExecution.ts#L10)

```ts
export type LocalCapabilityId = "youtube-audio";
```

## `LocalExecutionClient` {#localexecutionclient}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/bot-sdk/src/localExecution/contracts.ts#L97)

```ts
export interface LocalExecutionClient {
    /** Interaction contexts capture their original lifetime; source tasks acquire a fresh bot connection. */
    executor(context: LocalRequestContext): LocalExecutor;
    /** Retained metadata outlives this invocation/signal, but never authorizes a replacement human socket. */
    retainSource(invocationId: string, url: string, options?: LocalExecutionTaskOptions): Promise<LocalSourceContext>;
    releaseSource(sourceContextId: string): Promise<void>;
    /**
     * Check the original physical executor's current voice/access without starting
     * client work. Rejects with LocalExecutionRpcError when unavailable; a later
     * stream must still pass fresh admission and consent.
     */
    checkSourceAvailability(sourceContextId: string, voiceChannelId: string, options?: LocalExecutionTaskOptions): Promise<void>;
}
```

## `LocalExecutionError` {#localexecutionerror}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/bot-sdk/src/localExecution/contracts.ts#L27)

```ts
export class LocalExecutionError extends Error {
    readonly event: Readonly<LocalExecutionTerminalEvent>;
    constructor(event: LocalExecutionTerminalEvent);
}
```

## `LocalExecutionProvider` {#localexecutionprovider}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/bot-sdk/src/localExecution/contracts.ts#L116)

```ts
export interface LocalExecutionProvider {
    localExecution(serverId: string): LocalExecutionClient;
}
```

## `LocalExecutionRpcError` {#localexecutionrpcerror}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/bot-sdk/src/localExecution/contracts.ts#L45)

```ts
export class LocalExecutionRpcError extends Error {
    readonly reason?: LocalTaskFailureReason;
    readonly cancellationCause?: LocalTaskCancellationCause;
    constructor(code: ProtocolErrorCode, message: string);
    readonly code: ProtocolErrorCode;
}
```

## `LocalExecutionTaskOptions` {#localexecutiontaskoptions}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/bot-sdk/src/localExecution/contracts.ts#L14)

```ts
export interface LocalExecutionTaskOptions {
    readonly signal?: AbortSignal;
}
```

## `LocalExecutionTerminalEvent` {#localexecutionterminalevent}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/bot-sdk/src/localExecution/contracts.ts#L24)

```ts
export type LocalExecutionTerminalEvent = {
    taskId: string;
    state: "failed";
    reason: "timeout" | "busy" | "invalid_request" | "permission_denied" | "executor_unavailable" | "unsupported_platform" | "tools_missing" | "tool_install_failed" | "integrity_failed" | "storage_failed" | "provider_unavailable" | "transport_failed" | "worker_failed";
    sourceFailure?: {
        code: "input" | "unsupported" | "tools" | "runtime" | "unavailable" | "timeout" | "busy";
    } | {
        code: "recovery_failed";
        attempts: number;
    } | undefined;
} | {
    taskId: string;
    state: "cancelled";
    cause: "requested" | "requester_left_voice" | "requester_disconnected" | "bot_left_voice" | "bot_disconnected" | "permission_revoked" | "source_released" | "voice_mode_changed" | "expired" | "server_shutdown";
};
```

## `LocalExecutor` {#localexecutor}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/bot-sdk/src/localExecution/contracts.ts#L80)

```ts
export interface LocalExecutor {
    execute(spec: Extract<LocalTaskSpec, {
        operation: 'youtube.search';
    }>, options?: LocalExecutionTaskOptions): Promise<Extract<LocalWireTaskResult, {
        operation: 'youtube.search';
    }>>;
    execute(spec: Extract<LocalTaskSpec, {
        operation: 'youtube.resolve';
    }>, options?: LocalExecutionTaskOptions): Promise<Extract<LocalWireTaskResult, {
        operation: 'youtube.resolve';
    }>>;
    execute(spec: Extract<LocalTaskSpec, {
        operation: 'youtube.preview';
    }>, options?: LocalExecutionTaskOptions): Promise<LocalWirePreviewResult>;
    execute(spec: LocalMetadataTaskSpec, options?: LocalExecutionTaskOptions): Promise<LocalMetadataTaskResult>;
    /** Resolve only after the private peer and reliable data channel are ready. */
    stream(spec: Extract<LocalTaskSpec, {
        operation: 'youtube.stream';
    }>, options: LocalStreamOptions): Promise<LocalOpusStream>;
}
```

## `LocalMediaTrack` {#localmediatrack}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/localExecution.ts#L76)

```ts
export type LocalMediaTrack = {
    id: string;
    title: string;
    url: string;
    duration: number;
};
```

## `LocalMetadataTaskResult` {#localmetadatataskresult}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/bot-sdk/src/localExecution/contracts.ts#L23)

```ts
export type LocalMetadataTaskResult = {
    operation: "youtube.search";
    tracks: {
        id: string;
        title: string;
        url: string;
        duration: number;
    }[];
} | {
    operation: "youtube.resolve";
    track: {
        id: string;
        title: string;
        url: string;
        duration: number;
    };
} | {
    localPreviewId: string;
    taskId: string;
    requestId: string;
    executorSessionId: string;
    operation: "youtube.preview";
};
```

## `LocalMetadataTaskSpec` {#localmetadatataskspec}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/bot-sdk/src/localExecution/contracts.ts#L22)

```ts
export type LocalMetadataTaskSpec = {
    operation: "youtube.search";
    query: string;
} | {
    url: string;
    operation: "youtube.resolve";
} | {
    url: string;
    operation: "youtube.preview";
};
```

## `LocalOpusStream` {#localopusstream}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/bot-sdk/src/localExecution/contracts.ts#L59)

```ts
export interface LocalOpusStream {
    /** Server wire task ID, never the executor's Main-local task UUID. */
    readonly taskId: string;
    readonly track: LocalMediaTrack;
    readonly frames: AsyncIterable<Uint8Array>;
    /** Aborted with LocalExecutionError on failure/cancellation, not normal EOF/drain. */
    readonly signal: AbortSignal;
    /** Rejects immediately on failure/cancellation; resolves after playback drain and server completion. */
    readonly closed: Promise<void>;
    /** Advance once after playback consumes a frame, never when buffering it. */
    markFrameAdvanced(): void;
    /** Await the matching revision; opposite requests supersede/reject older ones. An 8s timeout fails the task. */
    setPaused(paused: boolean): Promise<void>;
    /**
     * Cancel active work and await teardown, or await server completion after playback
     * has drained. Cancellation remains observable through signal/closed.
     * Does not release a retained source.
     */
    close(): Promise<void>;
}
```

## `LocalPreviewReference` {#localpreviewreference}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/localExecutionProtocol.ts#L141)

```ts
export type LocalPreviewReference = {
    localPreviewId: string;
    taskId: string;
    requestId: string;
    executorSessionId: string;
};
```

## `LocalRequestContext` {#localrequestcontext}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/localExecutionProtocol.ts#L98)

```ts
export type LocalRequestContext = {
    invocationId: string;
    kind: "invocation";
} | {
    requestId: string;
    kind: "autocomplete";
} | {
    requestId: string;
    kind: "audio-preview";
} | {
    kind: "source";
    sourceContextId: string;
};
```

## `LocalSourceContext` {#localsourcecontext}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/localExecutionProtocol.ts#L120)

```ts
export type LocalSourceContext = {
    url: string;
    sourceContextId: string;
    botId: string;
    botPublicKey: string;
    invokerId: string;
    invokerSessionId: string;
    originChannelId: string;
    capability: "youtube-audio";
    provider: "youtube-local";
    expiresAt: number;
};
```

## `LocalSourceFailure` {#localsourcefailure}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/localExecutionProtocol.ts#L280)

```ts
export type LocalSourceFailure = {
    code: "input" | "unsupported" | "tools" | "runtime" | "unavailable" | "timeout" | "busy";
} | {
    code: "recovery_failed";
    attempts: number;
};
```

## `LocalStreamOptions` {#localstreamoptions}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/bot-sdk/src/localExecution/contracts.ts#L18)

```ts
export interface LocalStreamOptions {
    readonly voiceChannelId: string;
    readonly signal?: AbortSignal | undefined;
}
```

## `LocalTaskCancellationCause` {#localtaskcancellationcause}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/localExecutionProtocol.ts#L275)

```ts
export type LocalTaskCancellationCause = "requested" | "requester_left_voice" | "requester_disconnected" | "bot_left_voice" | "bot_disconnected" | "permission_revoked" | "source_released" | "voice_mode_changed" | "expired" | "server_shutdown";
```

## `LocalTaskEvent` {#localtaskevent}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/localExecutionProtocol.ts#L318)

```ts
export type LocalTaskEvent = {
    taskId: string;
    state: "accepted";
    result: {
        operation: "youtube.search";
        tracks: {
            id: string;
            title: string;
            url: string;
            duration: number;
        }[];
    } | {
        operation: "youtube.resolve";
        track: {
            id: string;
            title: string;
            url: string;
            duration: number;
        };
    } | {
        localPreviewId: string;
        taskId: string;
        requestId: string;
        executorSessionId: string;
        operation: "youtube.preview";
    } | {
        operation: "youtube.stream";
        track: {
            id: string;
            title: string;
            url: string;
            duration: number;
        };
    };
    media?: {
        protocol: "opus-datachannel-v1";
        generation: number;
        iceServers: {
            urls: string[];
            username?: string | undefined;
            credential?: string | undefined;
        }[];
    } | undefined;
} | {
    taskId: string;
    state: "ready";
    mediaGeneration: number;
} | {
    revision: number;
    taskId: string;
    state: "paused";
} | {
    revision: number;
    taskId: string;
    state: "resumed";
} | {
    taskId: string;
    state: "completed";
    mediaGeneration?: number | undefined;
    playedFrames?: number | undefined;
} | {
    taskId: string;
    state: "failed";
    reason: "timeout" | "busy" | "invalid_request" | "permission_denied" | "executor_unavailable" | "unsupported_platform" | "tools_missing" | "tool_install_failed" | "integrity_failed" | "storage_failed" | "provider_unavailable" | "transport_failed" | "worker_failed";
    sourceFailure?: {
        code: "input" | "unsupported" | "tools" | "runtime" | "unavailable" | "timeout" | "busy";
    } | {
        code: "recovery_failed";
        attempts: number;
    } | undefined;
} | {
    taskId: string;
    state: "cancelled";
    cause: "requested" | "requester_left_voice" | "requester_disconnected" | "bot_left_voice" | "bot_disconnected" | "permission_revoked" | "source_released" | "voice_mode_changed" | "expired" | "server_shutdown";
};
```

## `LocalTaskFailureReason` {#localtaskfailurereason}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/localExecutionProtocol.ts#L277)

```ts
export type LocalTaskFailureReason = "timeout" | "busy" | "invalid_request" | "permission_denied" | "executor_unavailable" | "unsupported_platform" | "tools_missing" | "tool_install_failed" | "integrity_failed" | "storage_failed" | "provider_unavailable" | "transport_failed" | "worker_failed";
```

## `LocalTaskSpec` {#localtaskspec}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/localExecution.ts#L87)

```ts
export type LocalTaskSpec = {
    operation: "youtube.search";
    query: string;
} | {
    url: string;
    operation: "youtube.resolve";
} | {
    url: string;
    operation: "youtube.preview";
} | {
    url: string;
    operation: "youtube.stream";
};
```

## `LocalWirePreviewResult` {#localwirepreviewresult}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/localExecutionProtocol.ts#L154)

```ts
export type LocalWirePreviewResult = {
    localPreviewId: string;
    taskId: string;
    requestId: string;
    executorSessionId: string;
    operation: "youtube.preview";
};
```

## `LocalWireTaskResult` {#localwiretaskresult}

[Source and validation](https://github.com/MonkyOrg/Monky/blob/main/packages/shared/src/localExecutionProtocol.ts#L153)

```ts
export type LocalWireTaskResult = {
    operation: "youtube.search";
    tracks: {
        id: string;
        title: string;
        url: string;
        duration: number;
    }[];
} | {
    operation: "youtube.resolve";
    track: {
        id: string;
        title: string;
        url: string;
        duration: number;
    };
} | {
    localPreviewId: string;
    taskId: string;
    requestId: string;
    executorSessionId: string;
    operation: "youtube.preview";
} | {
    operation: "youtube.stream";
    track: {
        id: string;
        title: string;
        url: string;
        duration: number;
    };
};
```
